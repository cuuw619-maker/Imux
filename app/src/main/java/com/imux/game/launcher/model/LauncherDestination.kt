package com.imux.game.launcher.model

enum class LauncherDestination {
    RENDER,
    GAME,
    CONTROL,
    GAMEPAD,
    LAUNCHER,
    LAYOUTS,
    ABOUT,
    ACCOUNTS,
    IMUX_DIRECTORY,
    RESOURCES
}

enum class LauncherRoute {
    HOME,
    SETTINGS
}

enum class UiScale {
    COMPACT,
    NORMAL,
    LARGE
}
